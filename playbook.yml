---
- name: "Test playbook run"
  hosts: localhost
  become: no
  tasks:
    - name: Write log to file
      ansible.builtin.shell: |
        echo "123" >> /Users/aleksandrdiptan/Documents/1.log
        
